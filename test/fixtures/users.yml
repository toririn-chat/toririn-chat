<% users = %w(とりりん トリピー だらずさん) %>

<% users.each.with_index do |name, index| %>
user_<%= index %>:
  name: <%= name %>
  icon: <%= File.basename(Dir.glob("public/images/icons/**/*.png").sample) %>
<% end %>
