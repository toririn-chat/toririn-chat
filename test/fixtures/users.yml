<% users = %w(とりりん トリピー だらずさん) %>
<% users.each.with_index do |name, index| %>
user_<%= index %>:
  email: <%= "test_#{index}@example.net" %>
  encrypted_password: <%= User.new.send(:password_digest, index.to_s * 8) %>
  confirmed_at: <%= DateTime.now %>
<% end %>
